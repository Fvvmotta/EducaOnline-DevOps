# ============================================
# Financeiro API - Deployment e Service
# ============================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: financeiro-api
  namespace: educaonline
  labels:
    app: financeiro-api
    app.kubernetes.io/name: financeiro-api
    app.kubernetes.io/part-of: educaonline-platform
    app.kubernetes.io/component: api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: financeiro-api
  template:
    metadata:
      labels:
        app: financeiro-api
    spec:
      containers:
        - name: financeiro-api
          image: fermotta/educaonline-financeiro-api:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 80
          env:
            - name: ASPNETCORE_ENVIRONMENT
              valueFrom:
                configMapKeyRef:
                  name: educaonline-config
                  key: ASPNETCORE_ENVIRONMENT
            - name: ConnectionStrings__DefaultConnection
              valueFrom:
                secretKeyRef:
                  name: educaonline-secrets
                  key: SQLSERVER_CONNECTION_FINANCEIRO
            - name: MessageQueueConnection__MessageBus
              valueFrom:
                secretKeyRef:
                  name: educaonline-secrets
                  key: RABBITMQ_CONNECTION
            - name: JwtSettings__Secret
              valueFrom:
                secretKeyRef:
                  name: educaonline-secrets
                  key: JWT_SECRET
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "250m"
          livenessProbe:
            httpGet:
              path: /health
              port: 80
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: financeiro-api-service
  namespace: educaonline
  labels:
    app: financeiro-api
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: financeiro-api
